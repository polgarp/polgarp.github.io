Bespoke verbs, one file per post, named after the post's markdown file
(e.g. `2026-03-15-brain-fry-and-the-automation-trap.js`). Anything dropped here
is loaded automatically on that post and nowhere else.

Use this only for a verb one post needs. Anything reusable belongs in
`../verbs.js`, so other posts can reach it.
